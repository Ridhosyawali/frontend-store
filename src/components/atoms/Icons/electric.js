import Link from "next/link";
import React from "react";
import { FaBoltLightning } from "react-icons/fa6";
const ElectricIcon = ({ className }) => {
  return <FaBoltLightning className={`${className}`} />;
};

export default ElectricIcon;
